# llmtokens.github.io

Static site for browsing LLM pricing snapshots, filtering by provider/model/usage, and viewing day-over-day changes.

## Local preview
Serve the site from the repository root (for example):

```bash
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.
